namespace AVMTradeReporter.Models.Data.Enums
{
    /// <summary>
    /// Sort direction for list endpoints.
    /// </summary>
    public enum SortDirection
    {
        /// <summary>
        /// Descending order (largest / most recent first).
        /// </summary>
        Desc,
        /// <summary>
        /// Ascending order (smallest / oldest first).
        /// </summary>
        Asc
    }
}
